# Public base URL of the Strateva Payment Router API (public contract only).
# Every VITE_* variable is PUBLIC: it is embedded into the browser bundle.
# Never put secrets, tokens or private endpoints here.
# There is deliberately NO fallback URL: if this is missing or invalid, the
# app fails safe and sends no request.
VITE_API_URL=http://localhost:8000
